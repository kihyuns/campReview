*DBMS = 데이터베이스 매니지먼트 서비스
-인증/권한 검사
-동시 접근 제어
-무결성 제공

DBMS 종류 : 오라클, MS-SQL, DB2, Altibase, Tibero


SQL = DBMS에 공통으로 사용할 수 있는 명령문 작성 문법

BDMS에 일을 시키려면 명령을 해야 하는데 DBMS마다 명령이 달라서 곤란하다

그래서 SQL 명령문으로 명령을 한다


DBMS 전용문법을 다른 DBMS로 변환하는 작업을 migration 이라고 한다

