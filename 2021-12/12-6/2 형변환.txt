*형변환

값 저장
-작은 크기의 메모리 값은 큰 크기의 메모리에 저장할 수 있다

-큰 값을 작은 메모리에 저장할 때는 값이 짤린다 (형변환 필요)


정수          >          부동소수점 변수

byte		  float    (7자리)
short		  double (15자리)
int
long


*문자열을 primitive type으로 변환하는 방법

	      문자열
	    |-----------|
	test?value=100    (자동 형변환)        test()
  web     ___________________      spring     ___________________ exam3
browser	웹브라우저가 보내	                boot
	는 값은 항상 문자열
	          ||
	파일 업로드의 경우
	바이너리 데이터이다


명시적 형변환이 불가능할 땐 특별한 메서드를 사용한다